{
  "name": "decisionflow-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "DecisionFlow — Turn complex data into confident decisions",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "test:calculations": "npm --prefix server run test:calc",
    "postinstall": "npm --prefix client install && npm --prefix server install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
